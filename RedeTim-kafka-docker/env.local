# Auf eine LAN-Adresse setzen, wenn der Broker von einem anderen Rechner erreichbar sein soll --
# die Adresse steht in den advertised listeners, ein anderer Host erreicht 127.0.0.1 also nie.
ADVERTISED_HOST=127.0.0.1
