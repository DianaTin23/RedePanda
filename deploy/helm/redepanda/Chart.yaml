apiVersion: v2
name: redepanda
description: Browser chat over Redpanda, with OpenTelemetry metrics via an OTel Collector into Prometheus
type: application

# Chart version and application version are tracked separately on purpose: the chart can be
# fixed without the application changing, and vice versa.
version: 0.1.0
appVersion: "0.1.0"

keywords:
  - chat
  - redpanda
  - kafka
  - opentelemetry
  - prometheus
