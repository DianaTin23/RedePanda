<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <RootNamespace>RedeTim.Contracts</RootNamespace>
    </PropertyGroup>

    <ItemGroup>
        <!-- Here rather than in the backend and the console client: ClientConfig is the shared
             base of every producer, consumer and admin config, so KafkaSecurity maps onto it
             once for all of them. -->
        <PackageReference Include="Confluent.Kafka" />
    </ItemGroup>

</Project>
